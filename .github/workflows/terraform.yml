name: Terraform

on:
  push:
    branches:
      - "**"

  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: [self-hosted, windows, x64]

    # IMPORTANT: Your secrets are stored under this Environment
    environment: preprod

    defaults:
      run:
        working-directory: environments/preprod

    steps:

      # Checkout Repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # Check Azure Secrets
      - name: Check Azure Secrets
        shell: powershell
        run: |
          $missing = $false

          if ([string]::IsNullOrWhiteSpace("${{ secrets.AZURE_CLIENT_ID }}")) {
            Write-Host "ERROR: AZURE_CLIENT_ID is EMPTY"
            $missing = $true
          }
          else {
            Write-Host "AZURE_CLIENT_ID is available"
          }

          if ([string]::IsNullOrWhiteSpace("${{ secrets.AZURE_CLIENT_SECRET }}")) {
            Write-Host "ERROR: AZURE_CLIENT_SECRET is EMPTY"
            $missing = $true
          }
          else {
            Write-Host "AZURE_CLIENT_SECRET is available"
          }

          if ([string]::IsNullOrWhiteSpace("${{ secrets.AZURE_SUBSCRIPTION_ID }}")) {
            Write-Host "ERROR: AZURE_SUBSCRIPTION_ID is EMPTY"
            $missing = $true
          }
          else {
            Write-Host "AZURE_SUBSCRIPTION_ID is available"
          }

          if ([string]::IsNullOrWhiteSpace("${{ secrets.AZURE_TENANT_ID }}")) {
            Write-Host "ERROR: AZURE_TENANT_ID is EMPTY"
            $missing = $true
          }
          else {
            Write-Host "AZURE_TENANT_ID is available"
          }

          if ($missing) {
            Write-Error "One or more Azure secrets are missing."
            exit 1
          }

          Write-Host "All Azure secrets are available."

      # Azure Login
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
            }

      # Verify Azure Login
      - name: Check Azure Account
        shell: powershell
        run: |
          az account show

      # Set Azure Subscription
      - name: Set Azure Subscription
        shell: powershell
        run: |
          az account set `
            --subscription "${{ secrets.AZURE_SUBSCRIPTION_ID }}"

      # Verify Subscription
      - name: Verify Azure Subscription
        shell: powershell
        run: |
          az account show

      # Terraform Format
      - name: Terraform Format
        shell: powershell
        run: |
          terraform fmt -check -recursive

      # Terraform Init
      - name: Terraform Init
        shell: powershell
        run: |
          terraform init

      # Terraform Validate
      - name: Terraform Validate
        shell: powershell
        run: |
          terraform validate

      # Terraform Plan
      - name: Terraform Plan
        shell: powershell
        run: |
          terraform plan